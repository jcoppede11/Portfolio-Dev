---
import { COLORS } from '../../../config/theme';

interface Props {
    className?: string
}
const { className } = Astro.props;
---

<ol class=`${COLORS.LIST_DISC} ${className}`>
    <slot />
</ol>        
