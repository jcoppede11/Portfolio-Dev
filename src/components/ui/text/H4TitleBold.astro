---
interface Props {
  subtitle: string;
  className?: string;
}

const { subtitle, className = "" } = Astro.props;
---

<h4 class={`text-2xl font-semibold flex gap-x-3 items-center mb-4 md:mb-8 md:mt-12 ${className}`}>
    <slot name="icon" /> {subtitle}
</h4>
