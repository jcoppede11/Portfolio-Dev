---
import { COLORS } from "../../../config/globalStyles";

interface Props {
    title: string;
    className?: string;
}

const { title = "", className = "" } = Astro.props;
---

<h3 class={`${COLORS.TITLE_GRAY_TEXT} text-2xl lg:text-4xl ${className}`}>
    {title}
</h3>
